version: 2
jobs:
  install:
    docker:
      - image: circleci/python:3.7.4
        environment:
          DATABASE_URL: postgresql://POSTGRES_USER:POSTGRES_PWD@localhost:5432/POSTGRES_USER
      - image: circleci/postgres:9.4
        environment:
          POSTGRES_USER: circleci
          POSTGRES_PWD: test
          POSTGRES_DB: example
    working_directory: ~/repo
    steps:
      - checkout
      - restore_cache:
          keys:
            - v1-dependencies-{{ checksum "requirements.txt" }}
            - v1-dependencies-
      - run:
          name: install dependencies
          command: |
            pip install -r requirements/dev.txt --user
            flask db upgrade
            flask test
      - save_cache:
          paths: 
            - ./venv
          key: v1-dependencies-{{ checksum "requirements.txt" }}
workflows:
  version: 2
  build-and-test:
    jobs:
      - install
